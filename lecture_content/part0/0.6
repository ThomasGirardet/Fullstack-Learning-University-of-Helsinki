```mermaid
sequenceDiagram
    participant browser
    participant server

    Note over browser: User writes a new note and clicks Save
    Note right of browser: SPA creates new note object and updates page locally 

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa (note=...)
    activate server
    server-->>browser: 201 Created
    deactivate server

    Note over browser: SPA updates the DOM without reloading the page
```