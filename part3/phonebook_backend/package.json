{
  "name": "phonebook_backend",
  "version": "0.0.1",
  "description": "",
  "license": "ISC",
  "author": "Thomas Girardet",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "del dist && cd ../../part2/phonebook_frontend && npm run build && robocopy dist ../../part3/phonebook_backend/dist /s",
    "deploy:full": "npm run build:ui || echo Build failed && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
